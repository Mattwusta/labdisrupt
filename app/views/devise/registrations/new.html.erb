<center><div class="page-header">
   <font color="gray" size ="6" face = "Baskerville Old Face">Join lab DISRUPT, We Might Just Change The World A Tiny Bit </font>
          <font color = "white" style="padding-left:60px;" > <%= link_to '註冊', new_user_registration_path, class: "btn btn-large
   btn-primary" %></font>
        </font>
  </center>

<h2>註冊</h2>

<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  
  	<%= f.input :name, :required => true, :autofocus => true %>
    <%= f.input :email, :required => true, :autofocus => true %>
    <%= f.input :password, :required => true %>
  
  </div>

  <div class="form-actions">
    <%= f.button :submit, "註冊" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
